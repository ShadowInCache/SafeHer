# SafeHer dependencies. Mixes three concerns in one file — see DEPENDENCIES.md for
# which packages belong to the current backend (fastapi_app/), the archived legacy
# gateway (legacy_flask_gateway/, flask/flask-cors/flask-sock/paho-mqtt only), and the
# ML training pipeline (ml_training/, everything from numpy down to soundfile).

# Web Framework
flask==3.0.0
flask-cors==4.0.0
fastapi==0.110.0
# Required for multipart/form-data, i.e. every file upload -- evidence
# uploads 400 without it. Was only in requirements-backend.txt.
python-multipart==0.0.9
uvicorn[standard]==0.27.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pydantic-settings==2.15.0
httpx==0.27.0
redis[hiredis]==5.0.1
asyncpg==0.29.0
alembic==1.13.1  # Database migrations
cloudinary==1.37.0

# Event Processing
paho-mqtt==1.6.1

# ML & Data Processing
numpy==1.24.3
scikit-learn==1.3.0
joblib==1.3.2
scipy==1.11.1

# GPU-Accelerated ML Training
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0
xgboost>=2.0.0
ultralytics>=8.0.0

# Image Processing (for weapon detection)
opencv-python==4.8.0.76
Pillow==10.0.0

# Audio Processing (for voice analysis) 
librosa==0.10.1
soundfile==0.12.1

# Utilities
python-dotenv==1.0.0
requests==2.31.0
asyncio-mqtt==0.13.0
supabase==2.0.2  # Cloud database REST API
google-auth==2.38.0

# Data handling
pandas==2.0.3

# Database ORM
sqlalchemy==2.0.23
aiosqlite==0.19.0

# Testing (README/SETUP.md both instruct `pytest tests/`, but nothing here
# actually installed it)
pytest==9.1.1
pytest-asyncio==1.4.0
